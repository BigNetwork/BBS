<h1><%= t('Editing') %> cart_row</h1>

<% form_for(@cart_row) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :f => f } %> 
  <p>
    <%= f.submit t('Update') %>
  </p>
<% end %>

<%= link_to t('Show'), @cart_row %> |
&larr; <%= link_to t('Back'), cart_rows_path %>