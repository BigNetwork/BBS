<h1>Editing cart</h1>

<% form_for(@cart) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @cart %> |
<%= link_to 'Back', carts_path %>