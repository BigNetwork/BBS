<li>
  <%= f.label :product_type_id, t('Product Type')+':' %>
  <%= f.collection_select :product_type_id, ProductType.all(:include => :children, :order => :name), :id, :name %>
  <%# TODO: Only allow the user to select actual products for delivery, not special/grouped/combo products. %>
</li>
<li>
  <%= f.label :quantity, t('Quantity')+':' %>
  <%= f.text_field :quantity %>
</li>