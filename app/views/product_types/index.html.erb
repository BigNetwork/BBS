<h1>Listing <%= controller.controller_name.humanize.downcase %></h1>

<table class="index_table">
  <tr>
    <th>Name</th>
    <th>Combination product?</th>
    <th>In stock</th>
    <th>Standard price</th>
    <th>Crew price</th>
  </tr>

<% @product_types.each do |product_type| %>
  <tr>
    <td><%=h product_type.name %></td>
    <td><%= "Ja" unless product_type.children.empty? %></td>
    <td><%= product_type.quantity_in_stock %></td>
    <td><%=h product_type.standard_price %></td>
    <td><%=h product_type.crew_price %></td>
    <td><%= link_to 'Show', product_type %></td>
    <td><%= link_to 'Edit', edit_product_type_path(product_type) %></td>
    <td><%= link_to 'Destroy', product_type, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New product_type', new_product_type_path %>