<h1>New <%= controller.controller_name.humanize.singularize.downcase %></h1>

<% form_for(@product_type) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', product_types_path %>