<p>
  <b>Name:</b>
  <%=h @product_type.name %>
</p>

<p>
  <b>Description:</b>
  <%=h @product_type.description %>
</p>

<p>
  <b>Standard price:</b>
  <%=h @product_type.standard_price %>
</p>

<p>
  <b>Crew price:</b>
  <%=h @product_type.crew_price %>
</p>

<% if @product_type.children.count > 0 %>
  
  Detta specialerbjudande består av följande 
  <% if @product_type.children.count > 1 %>
    <%= @product_type.children.count %> produkter:
  <% else %>
    produkt:
  <% end %>

  <ul>
  <% for child in @product_type.children %>
    <li><%= link_to h(child.name), child %></li>
  <% end %>
  </ul>

<% end %>

<% if @product_type.parents.uniq.count > 0 %>
  
  Denna produkt är en del i följande 
  <% if @product_type.parents.uniq.count > 1 %>
    <%= @product_type.parents.uniq.count %> specialerbjudanden:
  <% else %>
    specialerbjudande:
  <% end %>

  <ul>
  <% for parent in @product_type.parents.uniq %>
    <li><%= link_to h(parent.name), parent %></li>
  <% end %>
  </ul>

<% end %>

<%= link_to 'Edit', edit_product_type_path(@product_type) %> |
<%= link_to 'Back', product_types_path %>